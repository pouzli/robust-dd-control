# Архив результатов

Здесь собраны неосновные артефакты:

- `baseline_v1/` — baseline-эксперименты;
- `experiments_v2/` — отдельная серия сравнительных прогонов;
- `notebook_05_basis_sweep_smoke/` — smoke-прогон;
- `linear_damped/` — локальные результаты по одной системе;
- `legacy_root/` — старые артефакты, которые раньше лежали прямо в `results/`.

Основные актуальные результаты вынесены в:

- `../main_pipeline/`
